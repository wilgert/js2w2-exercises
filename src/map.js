const circumference = solarSystem => {
  /* add code here that calculates the circumference of each planet 
     don't forget to use the .map method
     Tip: π is represented by Math.PI in JavaScript */
};

module.exports = { circumference };
